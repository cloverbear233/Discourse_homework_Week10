# 请勿上传到 Git 远程仓库的本地文件说明

以下内容**已通过仓库根目录 `.gitignore` 排除**。发布到 GitHub 前请**不要**使用 `git add -f` 强制纳入版本库。

| 类型 | 路径 / 文件名 | 说明 |
|------|----------------|------|
| 实验报告正文 | `实验报告.md` | 本地撰写即可；如需公开仓库可用 README 概括 |
| 课程报告（若有） | `课程报告.md`、`课程报告.pdf` | 同上 |
| 实验截图与附图 | `image/`（含 `image/实验报告/` 等子目录） | 截图文件名可能含本地信息；请勿提交 |
| 模型缓存 | `.hf_cache/` | 体积大且可重建；协作方本地自动生成 |

**以上文件请保留在你自己的磁盘上，不要删除**——只是不参与 Git 远程同步。

若截图路径写在 `实验报告.md` 中，本地预览不受影响；克隆仓库的其他人看不到对应图片（因目录未提交）。
